{
    "version": "0.2.0",
    "configurations": [
    {
        "type": "java",
        "name": "Run SimpleWeather",
        "request": "launch",
        "mainClass": "SimpleWeather"   // no package line, so just the class name
    }
    ]
}

